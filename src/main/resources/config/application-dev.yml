spring:
    devtools:
        restart:
            enabled: true
        livereload:
            enabled: false # we use gulp + BrowserSync for livereload
    datasource:
         url: jdbc:mysql://localhost:3306/jpa_2
         username: root
         password: test
    jpa:
       hibernate.ddl-auto: update
       generate-ddl: true
       show-sql: true
server:
     port: 8080
